# Локальная платформа для работы с языковыми моделями и векторными базами данных

Проект представляет собой полнофункциональную локальную платформу для работы с языковыми моделями (LLM) и векторными базами данных. Платформа объединяет мощный сервер Ollama для запуска локальных языковых моделей и высокопроизводительную векторную базу данных Qdrant, предоставляя готовое решение для разработки AI-приложений.

## 🏗️ Архитектура проекта

Проект построен на основе Docker Compose и включает следующие компоненты:

### Основные сервисы:

- **Ollama** - Сервер для запуска и управления локальными языковыми моделями
  - Порт: `11434`
  - Предустановленные модели: `nomic-embed-text:@latest` и `qwen2.5-coder:@latest`
  - Данные сохраняются в Docker volume `ollama_data`

- **Qdrant** - Высокопроизводительная векторная база данных
  - HTTP порт: `6333`
  - gRPC порт: `6334`
  - Веб-интерфейс доступен по адресу: `http://localhost:6333/dashboard`
  - Данные сохраняются в Docker volume `qdrant_data`

### Сетевые настройки:
- Все сервисы объединены в сеть `llm-network`
- Автоматическая настройка bridge-сети для взаимодействия между контейнерами

## 🚀 Быстрый старт

### Предварительные требования:
- Установленный Docker и Docker Compose
- Минимум 8 ГБ оперативной памяти (рекомендуется 16+ ГБ)
- 20 ГБ свободного места на диске для моделей

### Полная настройка проекта:

```bash
# Клонируйте проект (если необходимо)
# git clone <repository-url>
# cd <project-directory>

# Запустите полную настройку
make setup
```

Эта команда автоматически:
1. Запустит сервисы Ollama и Qdrant в фоновом режиме
2. Загрузит предустановленные языковые модели
3. Настроит все необходимые компоненты

### Ручная настройка:

```bash
# Запуск сервисов
docker-compose up -d

# Ожидание запуска Ollama
sleep 10

# Загрузка моделей
curl -X POST http://localhost:11434/api/pull -d '{"name": "nomic-embed-text"}'
curl -X POST http://localhost:11434/api/pull -d '{"name": "qwen2.5-coder"}'
```

## 🛠️ Использование Makefile

Проект включает удобный Makefile для управления всеми аспектами платформы:

### Основные команды управления сервисами:

```bash
make up        # Запуск сервисов в фоне
make down      # Остановка сервисов
make restart   # Перезапуск сервисов
make logs      # Просмотр логов сервисов в реальном времени
make status    # Проверка статуса сервисов
```

### Команды для работы с моделями:

```bash
make setup         # Полная настройка проекта (запуск + загрузка моделей)
make pull-models   # Загрузка моделей в уже запущенный Ollama
```

### Команды очистки:

```bash
make clean          # Остановка и удаление контейнеров
make clean-volumes  # Полная очистка с удалением томов (данные будут потеряны!)
```

### Информационные команды:

```bash
make urls    # Вывод всех URL для доступа к сервисам
make help    # Показать справку по всем командам
```

## 🌐 API эндпоинты

После запуска сервисы доступны по следующим адресам:

### Ollama:
- **API эндпоинт**: `http://localhost:11434`
- **Web UI**: `http://localhost:11434` (если доступен)

### Qdrant:
- **HTTP API**: `http://localhost:6333`
- **gRPC API**: `http://localhost:6334`
- **Dashboard**: `http://localhost:6333/dashboard`

## 🤖 Модели

Платформа включает две предустановленные модели:

### nomic-embed-text:@latest
- **Тип**: Модель для создания эмбеддингов (векторных представлений)
- **Размер**: 300 миллионов параметров
- **Назначение**: Преобразование текста в векторные представления для поиска и классификации
- **Использование**: Создание эмбеддингов для векторного поиска в Qdrant

### qwen2.5-coder:@latest
- **Тип**: Кодогенерирующая языковая модель
- **Размер**: 7 миллиардов параметров
- **Назначение**: Генерация и анализ кода, помощь в программировании
- **Использование**: Автоматизация кодирования, рефакторинг, отладка

## 💡 Примеры использования

### 1. Создание RAG-системы (Retrieval-Augmented Generation)

```python
import requests
import json

# Создание эмбеддингов через Ollama
def create_embedding(text):
    response = requests.post('http://localhost:11434/api/embeddings', 
                           json={'model': 'nomic-embed-text', 'prompt': text})
    return response.json()['embedding']

# Сохранение в Qdrant
def store_in_qdrant(text, embedding, collection_name="documents"):
    payload = {
        "points": [{
            "id": 1,
            "vector": embedding,
            "payload": {"text": text}
        }]
    }
    requests.put(f'http://localhost:6333/collections/{collection_name}/points', 
                json=payload)

# Поиск похожих документов
def search_similar(text, collection_name="documents"):
    embedding = create_embedding(text)
    payload = {
        "vector": embedding,
        "limit": 5
    }
    response = requests.post(f'http://localhost:6333/collections/{collection_name}/points/search', 
                           json=payload)
    return response.json()
```

### 2. Генерация кода с помощью qwen2.5-coder

```python
import requests

def generate_code(prompt):
    payload = {
        "model": "qwen2.5-coder",
        "prompt": f"Напиши код на Python для: {prompt}",
        "stream": False
    }
    response = requests.post('http://localhost:11434/api/generate', json=payload)
    return response.json()['response']

# Пример использования
code = generate_code("функция для сортировки списка чисел")
print(code)
```

### 3. Семантический поиск документов

```python
def semantic_search(query, documents_collection):
    # Создание эмбеддинга запроса
    query_embedding = create_embedding(query)
    
    # Поиск в Qdrant
    payload = {
        "vector": query_embedding,
        "limit": 10,
        "with_payload": True
    }
    response = requests.post(
        f'http://localhost:6333/collections/{documents_collection}/points/search',
        json=payload
    )
    
    return response.json()['result']
```

## 🔧 Расширенная настройка

### Добавление новых моделей в Ollama:

```bash
# Просмотр доступных моделей
curl http://localhost:11434/api/tags

# Загрузка дополнительной модели
curl -X POST http://localhost:11434/api/pull -d '{"name": "llama2"}'

# Удаление модели
curl -X DELETE http://localhost:11434/api/delete -d '{"name": "model-name"}'

# Создание алиаса модели (копия с простым именем)
curl -X POST http://localhost:11434/api/copy \
  -d '{
    "source": "nomic-embed-text",
    "destination": "nomic-embed-text"
  }'
```

### Работа с коллекциями в Qdrant:

```bash
# Создание коллекции
curl -X PUT http://localhost:6333/collections/test_collection \
  -H "Content-Type: application/json" \
  -d '{
    "vectors": {
      "size": 768,
      "distance": "Cosine"
    }
  }'

# Просмотр коллекций
curl http://localhost:6333/collections
```

## 🛡️ Безопасность и производительность

### Рекомендации по безопасности:
- Сервисы запускаются в изолированных контейнерах
- Доступ только через localhost по умолчанию
- Для production использования настройте брандмауэр и аутентификацию

### Оптимизация производительности:
- Используйте SSD для хранения данных моделей
- Настройте размеры томов в зависимости от количества моделей
- Мониторьте использование памяти при работе с большими моделями

## 🤝 Разработка и вклад

Проект построен на стандартных технологиях и легко расширяется:

- Добавление новых моделей через API Ollama
- Интеграция с другими векторными базами данных
- Поддержка различных фреймворков ML/AI

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи сервисов: `make logs`
2. Убедитесь, что порты не заняты другими приложениями
3. Проверьте доступное место на диске для загрузки моделей

---

**Примечание**: Первый запуск может занять значительное время из-за загрузки моделей (несколько гигабайт каждая).

---
